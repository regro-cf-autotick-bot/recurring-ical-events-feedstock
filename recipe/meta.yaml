{% set name = "recurring-ical-events" %}
{% set version = "3.0.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/recurring_ical_events-{{ version }}.tar.gz
  sha256: 7131800dc6a7c6e1f534d6106a1924a00e131ccd276ac8cbe3504b688b384e03

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python >=3.9
    - pip 
  run:
    - python >=3.9
    - icalendar >=5.0.0,<7.0.0
    - python-dateutil >=2.8.1,<3.0.0
    - x-wr-timezone ==0.*
    - tzdata >=2024a

test:
  imports:
    - recurring_ical_events
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/niccokunzmann/python-recurring-ical-events
  summary: A Python module which repeats ICalendar events by RRULE, RDATE and EXDATE.
  license: LGPL-3.0-or-later
  license_file: LICENSE

extra:
  recipe-maintainers:
    - tdido
